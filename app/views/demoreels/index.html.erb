<h1>Demoreels</h1>

<table class="table index">
  <thead>
    <tr>
      <th><%= trans_cap('title') %></th>
    </tr>
  </thead>

  <tbody>
    <% @demoreels.each do |demoreel| %>
      <tr>
        <td class="title">
          <%= link_to edit_demoreel_path(demoreel) do %>
            <%= demoreel.title %>
          <% end %>
          <span class="actions">
            <%= link_to edit_demoreel_path(demoreel)  do %>
              <%= fa_icon "edit", title: trans_cap('data.edit') %>
            <% end %>
            <%= link_to demoreel, method: :delete, data: { confirm: trans_cap('data.confirm') } do %>
              <%= fa_icon "trash-o", title: trans_cap('data.delete') %>
            <% end %>
          </span>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to new_demoreel_path do %>
  <%= fa_icon "plus", title: trans_cap('data.add'), text: trans_cap('data.add') %>
<% end %>
