<%= form_for(@demoreel) do |f| %>
  <% if @demoreel.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@demoreel.errors.count, "error") %> prohibited this demoreel from being saved:</h2>

      <ul>
      <% @demoreel.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :link %><br>
    <%= f.text_field :link %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
