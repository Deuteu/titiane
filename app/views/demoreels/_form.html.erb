<%= form_for(@demoreel) do |f| %>
  <% if @demoreel.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@demoreel.errors.count, t('error')) %> prohibited this demoreel from being saved:</h2>

      <ul>
      <% @demoreel.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title, trans_cap('title') %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :link, trans_cap('link') %><br>
    <div class="format">http://player.vimeo.com/video/&lt;id&gt;</div>
    <%= f.text_field :link %>
  </div>
  <div class="actions">
    <%= f.submit trans_cap('submit') %>
  </div>
<% end %>
